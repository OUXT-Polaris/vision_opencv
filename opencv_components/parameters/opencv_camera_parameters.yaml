opencv_camera_components:
  camera_id: {
    type: int,
    default_value: 0,
    read_only: true,
    description: "ID of the camera you want to get images.",
    validation: { gt_eq<>: [0.0] },
  }
